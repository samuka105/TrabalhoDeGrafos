#include <iostream>
#include "grafo_matriz.h"
#include "grafo_lista.h"

int main(int argc, char* argv[]) {
    // Interpreta os argumentos da linha de comando (e.g., -d, -c, -m)
    // Cria ou carrega o grafo com base na entrada
    // Exemplo: Carregar grafo de matriz
    /*GrafoMatriz g;
    g.carregaGrafo("tests/grafo1.txt");
    g.eh_bipartido();*/
}
